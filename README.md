# 📦 Console App: Warehouse, Order Manager, Service Center & Templates

Комплексный учебный проект консольного приложения на C++, демонстрирующий продвинутые принципы объектно-ориентированного программирования (ООП), обобщенное программирование (Templates) и работу со стандартной библиотекой шаблонов (STL).

---

## 📂 Структура Проекта

Код разделен на логические модули для соблюдения принципов чистого кода:

### 🏭 Модуль Склада (Warehouse)
* **Warehouse.h/cpp** — Логика инвентаризации, статистика, работа с файловой системой (`data.txt`, `log.txt`).
* **Product.h** — Структура товара.

### 📋 Модуль Заказов (OrderManager)
* **OrderManager.h/cpp** — Управление заказами, сортировка с использованием лямбда-выражений, генерация гарантий.
* **Order.h** — Структура заказа.
* **Customer.h** — Класс клиента.
* **Warranty.h** — Класс гарантии.

### 🛠️ Модуль Сервисного Центра (Polymorphism Demo)
* **ServiceCenter.h** — Управление очередью ремонта (`repairQueue`) с использованием умных указателей.
* **PolimorphDevice.h/cpp** — Иерархия классов:
    * Базовый класс **Device** (абстрактный).
    * Производные классы **Phone** и **Laptop**.

### 📦 Модуль Хранения (Templates Demo)
* **StorageRack.h** — Шаблонный класс стеллажа (`StorageRack<T>`), демонстрирующий обобщенное программирование. По умолчанию работает со `string`, но поддерживает любые типы данных.

### 📄 Основные файлы
* **main.cpp** — Точка входа. Реализует главное меню с 4 режимами работы.

---

## 🎓 Реализованные Технологии и Концепции

### 1. Шаблоны Классов (Templates)
Реализован класс `StorageRack<T>`, который позволяет создавать типизированные хранилища.
* Поддержка добавления элементов с проверкой вместимости (`addItem`).
* Удаление и получение элемента по индексу (`GetAndRemoveItem`).
* Демонстрация работы с шаблонным параметром по умолчанию (`template<typename T = string>`).

### 2. Управление Памятью (Smart Pointers)
Используются современные средства управления памятью для предотвращения утечек:
* **`std::unique_ptr`**: Используется в `GenerateWarranty` для создания уникальных объектов гарантии.
* **`std::shared_ptr`**:
    * В `OrderManager`: связывает клиента (`Customer`) с заказом.
    * В `ServiceCenter`: вектор `repairQueue` хранит `shared_ptr<Device>`, позволяя безопасно управлять полиморфными объектами в контейнере.

### 3. Полиморфизм
* Виртуальные методы (`Diagnose`).
* Работа с объектами наследников (`Phone`, `Laptop`) через указатель на базовый класс `Device`.
* Принцип подстановки в действии: сервисный центр чинит любые устройства, не зная их конкретного типа.

### 4. STL Контейнеры и Алгоритмы
* **`std::vector`**: Основной контейнер для очереди ремонта и списка товаров.
* **`std::map`**: Используется в `OrderManager` для хранения заказов (связка ID -> Order) и владельцев.
* **Лямбда-выражения и Сортировка**:
    * Использование `std::sort` с кастомным компаратором (лямбдой) для сортировки заказов по убыванию ID:
    ```cpp
    sort(sortedOrders.begin(), sortedOrders.end(), [](const auto& a, const auto& b) {return a.first > b.first;});
    ```

### 5. Обработка Исключений (Exception Handling)
Безопасное выполнение кода с блоками `try-catch`.
* Обработка `std::out_of_range` при доступе к несуществующему индексу в `StorageRack` или `ServiceCenter`.
* Обработка `std::runtime_error` при переполнении стеллажа.

---

## 🚀 Функционал Меню

1.  **Warehouse**: Управление товарами, цены, логи, файловый ввод/вывод.
2.  **OrderManager**: Создание заказов, сортировка, выдача гарантий, привязка клиентов.
3.  **Polymorphism Demo**: Диагностика устройств, очередь ремонта (добавление/починка).
4.  **Template Demo**: Работа с шаблонным стеллажом (добавление, удаление товаров, просмотр).

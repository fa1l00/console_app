# 📦 Console Warehouse & Order Manager

Учебный проект консольного приложения на C++, разработанный для освоения принципов объектно-ориентированного программирования (ООП), работы с памятью и стандартной библиотекой шаблонов (STL).

## 📂 Структура Проекта

Код организован в соответствии со стандартами C++ (разделение объявлений и реализации):

### 📄 Исходные файлы (`Исходные файлы`)
* **`main.cpp`** — Точка входа в программу. Реализует главное меню и выбор режима работы.
* **`Warehouse.cpp`** — Логика работы склада: управление инвентарем, поиск, статистика.
* **`OrderManager.cpp`** — Логика обработки заказов: создание, хранение, сортировка.

### 📑 Заголовочные файлы (`Файлы заголовков`)
* **`Product.h`** — Описание структуры товара (сущность).
* **`Order.h`** — Описание структуры заказа (сущность).
* **`Warehouse.h`** — Интерфейс класса управления складом.
* **`OrderManager.h`** — Интерфейс класса управления заказами.

### 💾 Файлы ресурсов (`Файлы ресурсов`)
* **`data.txt`** — База данных для загрузки товаров при старте.
* **`log.txt`** — Файл для автоматического сохранения истории операций.

---

## 🎓 Изученные Технологии и Концепции

В ходе разработки я закрепил на практике следующие темы:

1.  **STL Контейнеры**:
    * `std::vector` — для хранения списков заказов и истории логов.
    * `std::map` / `std::unordered_map` — для быстрого поиска товаров по ID и подсчета статистики категорий.

2.  **Управление Памятью**:
    * **Умные указатели (`std::unique_ptr`)** — безопасная работа с объектами (реализовано в функции поиска `FindSmart`).

3.  **Алгоритмы и Лямбда-выражения**:
    * Использование `std::sort` с кастомными **лямбда-функциями** для сортировки заказов и вывода топ-дорогих товаров.

4.  **Работа с файлами (I/O)**:
    * Использование библиотеки `<fstream>` для чтения конфигов (`data.txt`) и записи логов (`log.txt`).

5.  **Принципы ООП**:
    * Инкапсуляция логики в классы `Warehouse` и `OrderManager`.

## 🚀 Как запустить
Скомпилируйте все `.cpp` файлы вместе. Убедитесь, что файлы `data.txt` и `log.txt` находятся в той же папке, что и исполняемый файл (или создайте их).

# 📦 Console App: Warehouse, Order Manager & Service Center

Комплексный учебный проект консольного приложения на C++, демонстрирующий продвинутые принципы объектно-ориентированного программирования (ООП). Проект объединяет управление складом, обработку заказов и симуляцию работы сервисного центра с использованием полиморфизма.

---

## 📂 Структура Проекта

Код разделен на логические модули для соблюдения принципов чистого кода и разделения ответственности.

### 🏭 Модуль Склада (Warehouse)
* **Warehouse.h/cpp** — Логика инвентаризации, логирование операций, работа с файловой системой (`data.txt`, `log.txt`).
* **Product.h** — Структура товара.

### 📋 Модуль Заказов (OrderManager)
* **OrderManager.h/cpp** — Управление заказами, сортировка, генерация гарантий.
* **Order.h** — Структура заказа.
* **Customer.h** — Класс клиента (работает в связке с `shared_ptr`).
* **Warranty.h** — Класс гарантии (работает в связке с `unique_ptr`).

### 🛠️ Модуль Сервисного Центра (Polymorphism Demo)
* **ServiceCenter.h** — Логика очереди ремонта, диагностика и починка устройств.
* **PolimorphDevice.h/cpp** — Иерархия классов устройств:
    * Базовый класс **Device**.
    * Производные классы **Phone** и **Laptop**.

### 📄 Основные файлы
* **main.cpp** — Точка входа. Реализует главное меню с выбором режима:
    1.  Склад
    2.  Заказы
    3.  Полиморфизм

---

## 🎓 Реализованные Технологии и Концепции

В этом обновленном проекте закреплены на практике следующие ключевые темы C++:

### 1. Управление Памятью (Smart Pointers)
Переход от "сырых" указателей к безопасным умным указателям:
* **`std::unique_ptr`**: Используется для генерации уникальных гарантий (`GenerateWarranty`) и поиска товаров (`FindSmart`), обеспечивая эксклюзивное владение ресурсом.
* **`std::shared_ptr`**: Используется для привязки клиентов к заказам и хранения очереди устройств в сервисном центре (`repairQueue`), позволяя объектам иметь нескольких владельцев.

### 2. Полиморфизм и Наследование
Реализована гибкая система типов для Сервисного Центра:
* Использование виртуальных функций (`virtual void Diagnose()`).
* Переопределение методов в классах-наследниках (**Phone**, **Laptop**).
* **Принцип подстановки**: `ServiceCenter` работает с абстракцией `Device`, не зная заранее, телефон это или ноутбук.

### 3. Обработка Исключений
* Использование блоков `try-catch` для безопасного выполнения кода.
* Генерация исключений `std::out_of_range` при попытке починить устройство по несуществующему индексу.

### 4. STL Контейнеры и Алгоритмы
* **`std::vector`** — для очереди ремонта и логов.
* **`std::map`** — для хранения заказов и владельцев.
* **`std::sort`** с **Лямбда-выражениями** — для кастомной сортировки данных.
